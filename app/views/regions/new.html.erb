<%= simple_form_for([@country, @region]) do |f| %>
  <%= f.input :title %>
  <%= f.input :description %>
  <%= f.input :image, as: :file %>
  <%= f.submit "Submit" %>
<% end %>

<p><%= link_to 'Back', country_path(@country) %></p>
