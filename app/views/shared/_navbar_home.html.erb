<% if user_signed_in? %>
  <div class="navbar">

    <div class="greeting">Hello <%= current_user.nickname %>. You can do it ! 💪😘</div>

    <div class="nav_margin">

      <% if policy(Country).create? %>
        <%= link_to 'Add a new country', new_country_path, class:"add_country"%>
      <% end %>

      <%= image_tag "faguoren_logo.jpg", class: "avatar dropdown-toggle", id: "navbarDropdown", data: { toggle: "dropdown" }, 'aria-haspopup': true, 'aria-expanded': false %>

      <div class="dropdown-menu dropdown-menu-right" aria-labelledby="navbarDropdown">
        <%= link_to "Log out", destroy_user_session_path, method: :delete, class: "dropdown-item" %>
      </div>

    </div>
  </div>
<% end %>
